<!-- Loop over each object in the projects array -->
{{#if posts.length}}
    <section>
        <h2>All Posts</h2>
        {{#each posts as |post| }}
            {{> postInfo post}}
        {{/each}}
    </section>
{{/if}}